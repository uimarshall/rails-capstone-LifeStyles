<h1>Articles#index</h1>
<p>Find me in app/views/articles/index.html.erb</p>
<div class="mother-feature">
  <div class="col-md-3 text-center">
    <%# <%= image_tag @user.article.avatar.url(:thumb), class: 'avatar-img' %> %>
  </div>
  <!--<div class="grid-h">
    <% @categories.each do |category| %>
      <div class='category'
             style="background-image: url(<%= latest_article_background(category) %>);">
        <p> <%= link_to category.name, category_path(category) %></p>
        <span> <%= category_article_link(category) %></span>
      </div>
    <% end %>
  </div> -->
  <h1>All Articles</h1>
  <ul id="articles">
    <% @articles.each do |article| %>
      <li>
        <%= article.title %>
      </li>
      <li>
        <%= article.text %>
      </li>
    <% end %>
  </ul>
  <% @categories.each do |category| %>
    <%= link_to category.name, articles_path(:cat => category.id) %>
  <% end %>
</div>
